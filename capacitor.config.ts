import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hypersync.ticketgenerator',
  appName: 'Hyper Ticket Generator',
  webDir: 'dist'
};

export default config;
